import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { App } from '@app/src/App';
import './index.css';

ReactDOM.render(
  <App name="React is ready..." />,
  document.getElementById('app')
)
